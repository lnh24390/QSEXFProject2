# 4시간 간격 학습 예약 점검 기록

## 전체 완료 확인

2026-09-14 13:35:26에 전체 파이프라인이 완료됐다. 사용자 진행 문의에 따라 상태와 최종 결과 JSON, 완료 이벤트 및 관련 프로세스 종료를 확인했다. 분할 마지막 미세조정은 15에포크에서 완료됐으나 마스크 검증 성능 비하락 조건을 통과하지 못해 3차 모델을 유지했다. 상세 비교는 `최종결과_요약.md`에 저장했다. 학습을 재시작하지 않았다.

완료에 따른 예약 점검 비활성화를 요청했으나, 앱에서 해당 예약이 존재하지 않아 변경할 수 없다는 응답을 받았다. 로컬 예약 파일도 발견되지 않았다. 이미 삭제됐을 가능성이 있으며, 새 예약을 만들지 않았다.

## 2026-09-14 04:55 KST

- 검출 3차 전이학습은 49에포크를 마치고 02:27:40에 종료했다. 후보 검증과 다음 단계 모델 선택은 02:29:02에 완료됐다.
- 같은 검증 자료의 mAP50–95는 입력 0.820744에서 후보 0.836451로 상승했다. 전체 점수와 클래스별 하락 제한 검사를 통과하여 3차 모델이 채택됐다.
- 기존 파이프라인이 검출 마지막 미세조정을 자동 시작했다. 상태 파일과 실제 명령줄에서 `pipeline.py bbox final` 실행을 확인했다. 현재 11에포크 완료, 12에포크 진행 중이며 학습 로그도 갱신되고 있다.
- 마지막 미세조정의 현재 최고 학습 중 검증 점수는 3에포크의 0.84071이다. 최근 11에포크 점수는 0.83908이다. 단계 완료 후 독립 후보 검증과 채택 판단은 아직 진행하지 않았다.
- 초기 모델의 동일 검증 기준 0.849193에는 아직 미치지 못한다. 최종 test 평가는 아직 대기 중이다.
- 파이프라인 PID 13500과 해당 작업의 실행 계층을 확인했다. 다른 학습 단계의 동시 실행은 확인되지 않았다. 학습을 중단하거나 재시작하지 않았다.
- 검출 마지막 미세조정 및 최종 평가 후 분할 2차 → 3차 → 마지막 미세조정과 최종 평가를 기존 파이프라인이 순차 수행한다. 데이터 형식과 설정을 변경하지 않았다.
- 조치: 자동 단계 전환이 정상임을 확인하여 실행을 유지했다. 다음 예약 점검도 4시간 간격으로 유지한다.

## 2026-09-14 08:56 KST

- 검출 마지막 미세조정은 13에포크에서 완료됐다. 후보 검증과 채택은 05:22:51, 최종 비교 기록은 05:23:21에 완료됐다. 최종 선택 모델의 val mAP50–95는 0.840917로 3차 입력보다 개선돼 채택됐으나, 초기 모델 0.849193에 미달하여 원본 회복 기준은 통과하지 못했다.
- 검출 최종 test mAP50–95는 0.741062다. 기존 동일 test 평가 기록의 1차 전이학습 0.681400보다 5.97%p 회복했지만 초기 모델 0.761066보다 2.00%p 낮다. 자동 배포하지 않았으며 원본 가중치를 유지한다. 한국 현장과 추가 난제의 별도 평가 부족은 여전히 남아 있다.
- 분할 2차는 12에포크에서 완료돼 06:49:50에 후보가 채택됐다. 박스와 마스크 mAP50–95 합은 입력 1.64309에서 후보 1.65397로 개선됐으며 클래스별 제한 검사도 통과했다.
- 현재 분할 YOLO11n-seg 3차가 14에포크 완료, 15에포크 진행 중이다. 평균은 초기 준비 에포크 제외 약 8.80분이다. 최고 합산 점수는 8에포크의 1.65601이며 해당 에포크 박스 mAP50–95는 0.84823, 마스크는 0.80777이다. 이는 학습 중 검증 수치이며 최종 채택 결과가 아니다.
- 상태 파일, 진행보고서, 결과 JSON, 갱신 중인 학습 로그와 실제 프로세스 명령줄을 교차 확인했다. 파이프라인 PID 13500, 현재 작업 실행 PID 10036 및 하위 Python PID 11504가 `pipeline.py seg stage3`를 실행한다. 다른 단계의 동시 학습은 확인되지 않았고 파이프라인 오류 로그는 비어 있다.
- 조치: 검출 완료 후 분할 2차와 3차로의 자동 순차 전환이 정상임을 확인했다. 재시작이나 설정 변경 없이 유지한다. 분할 3차 이후 마지막 미세조정과 최종 평가가 남아 있으므로 4시간 예약 점검을 계속 유지한다.
